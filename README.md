# Noam — Docusaurus (classic)

## Démarrer
```bash
npm install
npm run start
```

## Build
```bash
npm run build
npm run serve
```

## GitHub Pages
Configure le repo **hykrow/hykrow.github.io** → Settings → Pages → Source = `gh-pages`.
Puis :
```bash
GIT_USER=hykrow npm run deploy
```
